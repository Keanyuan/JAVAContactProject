spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Keanyuan/spring-cloud-config-repo.git
          username: keanyuan
          password: as9787105
          basedir: /Users/kean_qi/MavenBuild/config-repo


eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
server:
  port: 8083

management:
  endpoints:
    web:
      exposure:
        include: "*"